ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_MAPS_API_KEY=AIza...
TRIPADVISOR_API_KEY=...

# Google OAuth — paste the full JSON contents of credentials.json
GOOGLE_CREDENTIALS_JSON={"installed":{"client_id":"...","client_secret":"...",...}}

# Set after first OAuth login on Cloud Run — paste the full contents of token.json
GOOGLE_TOKEN_JSON={"token":"...","refresh_token":"...",...}

# Set to your Cloud Run service URL
REDIRECT_URI=https://your-service-url.run.app/api/auth/callback
